name: Tests

on:
  push:
    branches: [ trunk]
  pull_request:
    branches: [ trunk]

jobs:
  vanity:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - name: Run smoke tests
        run: ./test/smoke.sh
